---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myhello-world-app
  annotations:
    fluxcd.io/automated: "true"
    fluxcd.io/tag.myapp: glob:'*'
spec:
  selector:
    matchLabels:
      app: hello-k8s
  template:
    metadata:
      labels:
        app: hello-k8s
    spec:
      containers:
      - name: myapp
        image: macintoshprime/hello-kubernetes:6f41e099
        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
        ports:
        - containerPort: 8080
          name: http
